// use siyi_gimbl_camera_protocol::*;

fn main() -> Result<(), String> {
    // // Connect to gimbal
    // let config = SiyiConfig::default();
    // let mut client = SiyiClient::connect(&config)?;

    // // Get firmware version
    // let fw = client.get_firmware_version()?;
    // println!("Camera FW: 0x{:08X}", fw.camera_firmware_ver);

    // // Control gimbal
    // client.set_gimbal_attitude(0.0, -45.0)?;

    // // Take a photo
    // client.take_photo()?;

    Ok(())
}
